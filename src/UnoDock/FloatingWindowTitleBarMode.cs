namespace UnoDock;

/// <summary>Selects who owns the title bar of a native floating window.
/// In-surface floating windows always use the UnoDock caption.</summary>
public enum FloatingWindowTitleBarMode
{
    Custom,
    System
}
