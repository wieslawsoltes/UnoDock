<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="UnoDock.Themes.DockChromeResources">
    <ControlTemplate x:Key="UnoDock.ChromeButtonTemplate" TargetType="ContentControl">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}">
            <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Foreground="{TemplateBinding Foreground}" Padding="{TemplateBinding Padding}" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
        </Border>
    </ControlTemplate>
    <ControlTemplate x:Key="UnoDock.ChromeThumbTemplate" TargetType="Thumb">
        <Border Background="{TemplateBinding Background}"/>
    </ControlTemplate>
    <ControlTemplate x:Key="UnoDock.NavigatorListTemplate" TargetType="ListBox">
        <ScrollViewer x:Name="NavigatorScrollViewer" Padding="1" Background="{TemplateBinding Background}" HorizontalScrollBarVisibility="Disabled" HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Enabled" IsTabStop="False">
            <ItemsPresenter/>
        </ScrollViewer>
    </ControlTemplate>
    <DataTemplate x:Key="UnoDock.NavigatorItemTemplate">
        <TextBlock Text="{Binding Title}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
    </DataTemplate>
    <ItemsPanelTemplate x:Key="UnoDock.NavigatorItemsPanel">
        <StackPanel/>
    </ItemsPanelTemplate>
    <ControlTemplate x:Key="UnoDock.MenuRowTemplate" TargetType="MenuFlyoutItem">
        <Grid Background="{TemplateBinding Background}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="29"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border x:Name="PART_MenuGutter" BorderThickness="0,0,1,0" BorderBrush="White" IsHitTestVisible="False"/>
            <TextBlock Grid.Column="1" Text="{TemplateBinding Text}" Foreground="{TemplateBinding Foreground}" FontSize="{TemplateBinding FontSize}" Margin="6,0,10,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" IsHitTestVisible="False"/>
            <Border Grid.ColumnSpan="2" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" IsHitTestVisible="False"/>
        </Grid>
    </ControlTemplate>
    <ControlTemplate x:Key="UnoDock.MenuPresenterTemplate" TargetType="MenuFlyoutPresenter">
        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
            <ScrollViewer HorizontalScrollBarVisibility="Disabled" HorizontalScrollMode="Disabled" VerticalScrollBarVisibility="Auto" VerticalScrollMode="Enabled" IsTabStop="False">
                <ItemsPresenter/>
            </ScrollViewer>
        </Border>
    </ControlTemplate>
</ResourceDictionary>
