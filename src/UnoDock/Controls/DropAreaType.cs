using UnoDock.Internal;
using UnoDock.Layout;

namespace UnoDock.Controls;
public enum DropAreaType
{
    DockingManager = 0,
    DocumentPane = 1,
    DocumentPaneGroup = 2,
    AnchorablePane = 3
}
