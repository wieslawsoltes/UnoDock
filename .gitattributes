# Preserve byte-identical Windows public-probe observations, including CRLF.
# Treat CR as a line terminator, while still checking spaces/tabs at line ends.
contracts/visual-fixtures/*.xml -text whitespace=blank-at-eol,blank-at-eof,space-before-tab,cr-at-eol
