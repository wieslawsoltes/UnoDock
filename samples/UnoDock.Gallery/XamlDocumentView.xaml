<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="UnoDock.Gallery.XamlDocumentView">
    <Grid Padding="20" RowSpacing="12" Background="{ThemeResource LayerFillColorDefaultBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox Header="Document title" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.AutomationId="XamlDocumentTitle"/>
        <TextBox x:Name="Editor" Grid.Row="1" Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" VerticalAlignment="Stretch" FontFamily="Consolas" AutomationProperties.AutomationId="XamlDocumentEditor"/>
        <CheckBox Grid.Row="2" Content="Allow closing this document" IsChecked="{Binding CanClose, Mode=TwoWay}"/>
    </Grid>
</UserControl>
