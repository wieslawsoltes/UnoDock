<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" x:Class="UnoDock.Gallery.XamlToolEditor">
  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel Padding="12" Spacing="10">
      <TextBlock Text="TOOL POLICY" FontWeight="SemiBold" FontSize="11" Opacity="0.7"/>
      <TextBox Header="Title" Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AutomationProperties.AutomationId="XamlToolTitle"/>
      <CheckBox Content="Allow hide" IsChecked="{Binding CanHide, Mode=TwoWay}"/>
      <CheckBox Content="Allow auto-hide" IsChecked="{Binding CanAutoHide, Mode=TwoWay}"/>
      <CheckBox Content="Allow document tabs" IsChecked="{Binding CanDockAsTabbedDocument, Mode=TwoWay}"/>
      <TextBox Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="120" AutomationProperties.AutomationId="XamlToolText"/>
      <TextBlock Text="These values update the real tool model, commands and caption buttons. Hide a tool, then use Show tools to restore it." TextWrapping="Wrap" FontSize="12" Opacity="0.7"/>
    </StackPanel>
  </ScrollViewer>
</UserControl>
